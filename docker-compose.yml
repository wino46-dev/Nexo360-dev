services:
  mariadb:
    image: docker.io/bitnami/mariadb@sha256:080f43016353de960fbdc733f7f149fae8316382af4c479a4a8adab369861909
    environment:
      MARIADB_USER: "myuser"
      MARIADB_PASSWORD: "password"
      MARIADB_DATABASE: "mydb"
      MARIADB_ROOT_PASSWORD: "secret"
    networks:
      - sh360-red

  app:
    build:
      args:
        user: sammy
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: sh360-app
    expose:
      - 9000
    environment:
      DB_CONNECTION: mysql
      DB_HOST: mariadb
      DB_PORT: 3306
      DB_DATABASE: mydb
      DB_USERNAME: myuser
      DB_PASSWORD: password
    depends_on:
      - mariadb
    command: >
      sh -lc "mkdir -p /var/www/storage/logs /var/www/bootstrap/cache && chown -R www-data:www-data /var/www/storage /var/www/bootstrap/cache && chmod -R ug+rwX /var/www/storage /var/www/bootstrap/cache && exec php-fpm -F"
    tty: true
    container_name: sh360-app
    restart: always
    working_dir: /var/www/
    volumes:
      - ./:/var/www    
    networks:
      - sh360-red

  nginx:
    image: nginx:alpine
    tty: true
    container_name: sh360-nginx
    restart: always
    ports:
      - 8701:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - sh360-red

networks:
  sh360-red:
    driver: bridge